# Known limitations

This repository has no control-plane runtime, persistence, authentication, agent network protocol, cloud provisioning, GPU discovery, engine lifecycle, benchmark execution, optimizer, quality evaluator, capsule payload, web interface, container image, deployment automation, autoscaling, or continuous optimization. Provider and hardware support is a target matrix, not current certification.
