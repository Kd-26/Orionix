# Diagnostics

Diagnostic bundles use a versioned manifest, explicit file allowlist, sizes/digests, per-file redaction state, and mandatory preview before upload. Secrets, prompts, evaluation rows, model output, weights, provider credentials, and raw customer paths are excluded by default. Collection/upload is not implemented.
