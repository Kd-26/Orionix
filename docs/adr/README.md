# Architecture Decision Records

ADRs capture durable decisions and their tradeoffs. Use sequential filenames, status, context, decision, and consequences. Supersede rather than silently rewrite accepted decisions.

Current records cover the monorepo, customer-side execution, typed/versioned contracts, provider-neutral execution, outbound agent commands, a modular control plane, capability-adaptive execution, and risk-based certification.
