# Container boundaries

Future images may include a CPU development environment, customer agent, control plane, and separately governed engine worker. This milestone intentionally supplies documentation rather than runnable images: base OS policy, non-root users, image signing, SBOMs, registry, CUDA compatibility, and hardening requirements must be decided before Dockerfiles imply a production posture.
