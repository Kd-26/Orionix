# Deployment scaffolding

These directories reserve reviewable deployment-export targets. They contain no deployment automation, credentials, cluster clients, or runnable production manifests. Exporters must produce deterministic files, list required secret names without values, and require manual approval before use.
