[project]
name = "llmopt-domain"
version = "0.1.0.dev0"
description = "Business concepts and lifecycle states for Orionix"
requires-python = ">=3.12"
dependencies = ["llmopt-common"]

[tool.uv.sources]
llmopt-common = { workspace = true }

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
