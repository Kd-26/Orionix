# Domain

**Purpose:** stable domain primitives shared across Orionix. **Responsibilities:** typed identifiers and lifecycle enums. **Non-responsibilities:** serialization models, persistence, optimization logic, infrastructure discovery, or vendor integration. **Allowed dependencies:** Python standard library only. **Future:** evolve primitives conservatively; wire compatibility belongs in `schemas`.
