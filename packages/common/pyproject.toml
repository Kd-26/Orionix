[project]
name = "llmopt-common"
version = "0.1.0.dev0"
description = "Shared configuration, errors, and logging for Orionix"
requires-python = ">=3.12"
dependencies = [
  "pydantic-settings==2.10.1",
  "pydantic==2.11.7",
]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
