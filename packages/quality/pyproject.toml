[project]
name = "llmopt-quality"
version = "0.1.0.dev0"
description = "Quality validation ports for Orionix"
requires-python = ">=3.12"
dependencies = ["llmopt-schemas"]

[tool.uv.sources]
llmopt-schemas = { workspace = true }

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
